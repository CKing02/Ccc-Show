# Copy to .env.production (Vercel: set in Project Settings → Environment Variables)
# Required for production. Without it, sitemap / OG image absolute URLs fall back to http://localhost:3000

NEXT_PUBLIC_SITE_URL=https://YOUR-DOMAIN.TLD

# Optional
# NODE_ENV=production
